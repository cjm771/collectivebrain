describe("Token tests", () => {

  // creation
  it('creates pseudo random generated token');
  it('should reject if user and type don\'t exist.');
  it('should allow to store metadata for tokens (such as invite email and name)');
  // removal
  it('should delete tokens when requested');
});